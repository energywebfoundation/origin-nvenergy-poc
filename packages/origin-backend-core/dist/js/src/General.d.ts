export declare const FILE_SUPPORTED_MIMETYPES: string[];
export interface ISuccessResponse {
    success: boolean;
    message?: string;
}
export declare type onUploadProgressFunction = (progressEvent: ProgressEvent) => void;
